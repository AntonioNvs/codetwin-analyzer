[pytest]
testpaths =
    tests/unit
    tests/integration
markers =
    unit: testes unitários isolados (sem I/O real, sem rede)
    integration: testes de integração que exercitam múltiplos módulos em conjunto
